language: cpp

dist: trusty

compiler:
    - gcc
    - clang

before_install:
    - sudo apt-get -qq update
    - sudo apt-get install -y qtbase5-dev

before_script:
    - mkdir build
    - cd build
    - cmake ..

script: make

