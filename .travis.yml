language: cpp

dist: trusty

compiler:
    - gcc
    - clang

before_install:
    - sudo apt-get -qq update
    - sudo apt-get install -y libtinyxml2-dev

before_script:
    - mkdir build
    - cd build
    - cmake ..

script: make

